{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/GameCult/cultnet-rs/contracts/idunn.runtime-activation.schema.json",
  "title": "Idunn runtime activation",
  "description": "Canonical positional Idunn launch identity. Creation alone grants nothing; it becomes current process observation only on Idunn's authenticated observed-activation projection after actuator verification.",
  "type": "array",
  "prefixItems": [
    { "const": "idunn.runtime_activation.v1" },
    { "$ref": "#/$defs/sha256" },
    { "$ref": "#/$defs/sha256" },
    { "type": "integer", "minimum": 1 }
  ],
  "minItems": 4,
  "maxItems": 4,
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^sha256-[0-9a-f]{64}$"
    }
  }
}
